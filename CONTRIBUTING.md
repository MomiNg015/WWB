# 开发与文档维护约定

## 工作流程

1. 修改前阅读 [工程说明](game/README.md) 与 [项目开发约定](game/AGENTS.md)，确认影响的规则、数据和界面。
2. 先实现并验证规则逻辑，再补交互动画，最后完善界面。未核实的原站行为应保留说明。
3. 完成功能时同步相关文档和 `CHANGELOG.md` 的“未发布”部分。计划中的功能写入设计文档，不作为已完成功能记入更新日志。
4. 检查变更并提交。不要将数据库、备份、上传图片、凭证或本地环境文件提交到仓库。

## 每次需要更新什么

| 变化 | 同步文档 |
| --- | --- |
| 新功能、行为修复、兼容性变化 | `CHANGELOG.md`：写清使用者能观察到的变化 |
| 启动命令、依赖要求、目录与工程范围 | 根目录 `README.md`、`game/README.md` |
| 后台操作、权限、数据迁移和备份 | `game/docs/admin-guide.md`；设计变化同步 `admin-design.md` |
| 战斗规则或原站证据 | `game/docs/rule-audit.md`、`game/docs/research.md` |
| 动画和视觉交互 | `game/docs/animation-reference.md`、`game/design-qa.md` |
| 长期开发原则与用户偏好 | `game/AGENTS.md` |

只改错别字或内部整理，不必逐条扩充日志；影响使用方式或维护方式时必须记录。文档各自维护对应信息，避免把完整设计重复复制进更新日志。

## 验证

功能或规则代码修改后，在 `game` 目录运行：

```sh
npm run typecheck
npm test
npm run build
```

为规则、权限、持久化等实质行为添加针对性回归测试。界面修改需在浏览器中验证涉及的流程。纯文档修改检查路径、链接和事实即可，不需要重复运行整套游戏测试。

验证记录注明实际执行结果；失败或未执行要如实说明。测试通过不等于原版规则已被完全还原。

## 更新日志写法与交付

- 按需使用“新增”“修复”“变更”“迁移与限制”“文档”等分类，省略空分类。
- 每条说明发生了什么、对谁有影响；需要迁移时链接具体操作。
- 开发中写入“未发布”。确认一批变更交付后，将对应条目移到带日期的章节；有正式 Git 版本标签时使用标签，没有时使用可定位的提交记录。
- 保留历史记录，不把尚未完成的工作写成完成，不追补不存在的测试结果。
- 后台内容发布不需要每次修改代码日志：具体内容调整由数据库中的版本记录和操作日志追踪。代码侧发布、迁移机制变化则需要记录。

提交信息使用简短明确的说明，例如 `feat: add item management`、`fix: reject invalid defense cards`、`docs: add project changelog`。
