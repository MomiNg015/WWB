# 视觉与交互验收

## 操作提示补全（2026-09-19）

已根据公开客户端 A.jR 补充红色操作提示条和提示音。浏览器验证零MP选择冰并确认显示“MP不足”，保留选牌和原有资源；追加精灵之杖后正常进入防守响应。主动奇迹不再因MP不足而无法选择，确认时拦截命令。此项覆盖早期文档“零MP确认按钮禁用”的设计。类型检查与生产构建通过。

## 最新验收（2026-09-19，覆盖下方早期记录）

战斗规则与主要交互验收通过，完整原站逐状态/逐帧一致性仍未完成。以下早期“碰撞”“飞入”方案已移除，不是现行表现。

- 63项测试、类型检查、生产构建通过；包含30组九人复杂对局与100组基础对局。
- 正式 Socket.IO 修行：攻10结算后机器人HP46；机器人攻21，玩家守5，动画后HP50→34，并补牌进入下一回合。浏览器未捕获错误日志。
- 可控场景验证普通防御、安全、反射、光攻击禁用、零MP免耗组合、购买决定、复活与补牌。购买恢复原版粉色300×90按钮和“买/不买”文字；禁用玩家目标不再使资源栏变暗。
- 动画改用原版左右提示块、卡面顺序展示、上下遮片抽牌；复活展示神器后再恢复HP。主要战斗音效已本地化。
- 整体页面对照证据见 research/qa/；主要界面保留原版布局。结束/排行等未捕获到全部原站状态，极端多人动画链仍缺逐帧证据。
- 规则细节、来源与未核实行为分别见 docs/rule-audit.md、docs/research.md 和 docs/animation-reference.md。

## 战斗整改验证（2026-09-19）

- 新增按服务器事件顺序播放的攻守碰撞、伤害飘字、安全、反射/乱弹目标、抽牌、恢复、资源变化等展示，播放期间禁用提交，机器人等待播放时长再行动。
- 浏览器可控场景 `tests/battle.html` 实测：铁盾抵御10点攻击，显示伤害−6后HP44并播放新牌入场；神之盾显示安全；超级镜显示反射至攻击方；乱弹武剑显示实际随机目标。
- 光属性场景实测普通盾和火星戒指不可点击，彩虹帘可选；选中彩虹帘后铁盾解锁。取消它时自动移除不再合法的选择。
- 共享防御校验覆盖296种道具×7种攻击属性，并验证客户端判定与服务器接受/拒绝一致；原有100局机器人测试仍通过。
- 最终29项测试、类型检查、生产构建通过。正式Socket.IO修行流程实测精灵之杖攻12、铁铠守5，显示伤害−7 HP，结算后HP43；不是仅在静态场景中验证。
- 此次新增通用结算动画，不代表原站每种专属音效、动画曲线和未实现特殊能力均已完成一比一复原。

日期：2026-09-19。目标：https://godfield.net/ 。整体结果：**blocked（完整一致性验收未通过）**，已有可运行基础版本，不是全规则兼容声明。

## 证据

- `../research/screens/`：原站首页、模式菜单、私密房间、设置、十类图鉴和弹窗截图。
- `../research/*.html`：原站可见 DOM，用于提取画布、按钮、格子及图鉴位置。
- `../research/qa/home-final-comparison.png`：同为1280×800的原站/本地首页并列比较。
- `../research/qa/menu-desktop.png`、`weapons-final.png`、`battle-final.png`：本地核心页面。

## 已通过的范围

首页并列目视对照：背景裁切、Logo、姓名输入、诞生按钮、应用商店标志、页眉/页脚均对齐。修复了SVG图标颜色与手牌攻防文字乱码。

本地浏览器实际操作验证：姓名进入、修行开始、选防具与防御提交、承受攻击、组合攻击、服务器推进回合、退出弹窗、返回首页、图鉴分类切换。测试中发现并修复了服务器消耗卡牌后，旧选择状态在下一帧引用空数组导致白屏的问题。修复后已实际完成下一轮攻击/防御。

自动化：20项测试通过，含100个种子的完整机器人对局及独立真实服务端联机、权限、状态版本、隐藏手牌、重连、匹配、积分测试；TypeScript检查与生产构建通过。

## 未通过完整验收的原因

- 原站结束、复杂交易、全部状态组合没有足够同状态截图；不能把实现截图当作一致性证明。
- 战斗布局已近似，箭头、部分图标、选择可用态、9人密集排列及全部音效仍需逐项对照。
- 原站移动端观察到固定画布横向溢出；本地沿用此结构，但未完成全流程移动端验收。
- 八种语言已有数据，仍有动态中文回退，未逐语言验收。
- 原站浏览器后续图鉴操作超时，未取得最终同尺寸图鉴重拍；早期截图和DOM仍可用。
- 规则兼容差异见 `docs/research.md`，不可用视觉验收替代规则验证。
